# AWS
AWS_PROFILE=personal
AWS_REGION=ap-south-1

# Auth — MUST be set in prod, min 32 chars, use: openssl rand -hex 32
JWT_SECRET=your-strong-random-secret-min-32-chars

# WhatsApp Business API
WHATSAPP_VERIFY_TOKEN=your-verify-token
WHATSAPP_ACCESS_TOKEN=your-access-token
WHATSAPP_PHONE_NUMBER_ID=your-phone-number-id
# App Secret from Meta Developer Console — used to verify webhook payload signatures
WHATSAPP_APP_SECRET=your-whatsapp-app-secret

# App
STAGE=dev

# Local dev only — MongoDB (when running serverless offline, no AWS DynamoDB needed)
MONGODB_URI=mongodb://localhost:27017
TABLE_PREFIX=gramsathi-dev
S3_AUDIO_BUCKET=gramsathi-audio-dev
BEDROCK_MODEL_ID=anthropic.claude-3-haiku-20240307-v1:0
