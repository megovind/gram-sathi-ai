{
  "name": "gramsathi-backend",
  "version": "1.0.0",
  "description": "GramSathi AI – Python serverless backend deployment tooling",
  "scripts": {
    "deploy:dev": "serverless deploy --stage dev",
    "deploy:force": "serverless deploy --stage dev --force",
    "deploy:prod": "serverless deploy --stage prod",
    "dev": "serverless offline start --verbose",
    "remove": "serverless remove"
  },
  "devDependencies": {
    "serverless": "^4.6.3",
    "serverless-offline": "^14.4.0"
  }
}
